import 'testdir/yass/test.dart';
import 'package:flutter/material.dart';
import 'package:flutter/material.dart';

/* IDEAL_IMPORT_START */
/* IDEAL_IMPORT_END */

void main() {
  runApp(MyApp());
}

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: MyHomePage(title: 'Flutter Demo Home Page'),
      debugShowCheckedModeBanner: false,
    );
  }
}

class MyHomePage extends StatefulWidget {
  MyHomePage({Key key, this.title}) : super(key: key);

  final String title;

  @override
  Main createState() => Main();
}

class Main extends State<MyHomePage> {
void ecfdafacceebCodeLink() {
const int DXAeVXGoifSMiXN = 314;
hihi_onPressed = DXAeVXGoifSMiXN;hihi_child = DXAeVXGoifSMiXN;final xNgrwQtAsPtfKRD = this.Int_Int__TemplateT(DXAeVXGoifSMiXN, DXAeVXGoifSMiXN);

}
void variableInit() {
/* IDEAL_INITIALISATION_START */
name = new Column(
  children: <Widget>[
  hihi,

  ],
);
/* IDEAL_INITIALISATION_END */
/* IDEAL_INITIALISATION_START */
hihi = new SizedBox(
  width: hihi_ideal_width,
  height: hihi_ideal_height,
  child: FlatButton(
    onPressed: hihi_ideal_enable ? hihi_ideal_onPressed : null,
      child: Text(
      hihi_ideal_text,
      style: TextStyle(
      fontSize: 16
    ),
  ),
  color: Color.fromRGBO(33, 8, 174, 1),
  textColor: Colors.white
),
);
/* IDEAL_INITIALISATION_END */

}
	var name;

	var hihi; var hihi_ideal_onPressed; var hihi_ideal_text = "salut"; var hihi_ideal_enable = true; var hihi_ideal_width = 80; var hihi_ideal_height = 40;

  int _counter = 0;

  _MyHomePageState() {
    /* IDEAL_INITIALISATION_CODELINK_START */
    /* IDEAL_INITIALISATION_CODELINK_END */
    /* IDEAL_INITIALISATION_START */
    /* IDEAL_INITIALISATION_END */
  }

  /* IDEAL_VARIABLE_START */
  /* IDEAL_VARIABLE_END */

  void _incrementCounter() {
    setState(() {
      _counter++;
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text(widget.title),
        ),
        body:
        /* IDEAL_BODY_START */
            Text("Start")
        /* IDEAL_BODY_END */

        );
  }
}
